services:
  - type: web
    name: frontdoor-platform-phse2-integration
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
